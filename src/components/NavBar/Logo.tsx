import React from 'react';
import styled from 'styled-components';

const LogoWrapper = styled.div`
  cursor: pointer;
  font-weight: 800;
  font-size: 2em;
  color: #FDFFFC;
`;

const Logo = () => <LogoWrapper>Chatterly</LogoWrapper>

export default Logo;